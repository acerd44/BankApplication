@page
@model BankWeb.Pages.CustomersModel
@{
}
<section id="hero">
    <div class="container text-start">
        <h1 class="text-start">Customers <h2>Page @Model.CurrentPage of @Model.PageCount</h2></h1>

    </div>

    <div class="container">
        <form class="text-start" method="get">
            <input asp-for="@Model.Search" />
            <input type="hidden" />
            <input type="submit" value="Search" />
        </form>
        <table class="table table-dark">
            <td class="col">
                Id
                <a asp-page="/Customers" asp-route-search="@Model.Search" asp-route-sortColumn="Id" asp-route-sortOrder="asc" style="text-decoration: none">
                    <i class="fa-solid fa-angle-up"></i>
                </a>
                <a asp-page="/Customers" asp-route-search="@Model.Search" asp-route-sortColumn="Id" asp-route-sortOrder="desc" style="text-decoration: none">
                    <i class="fa-solid fa-angle-down"></i>
                </a>
            </td>
            <td class="col">
                Name
                <a asp-page="/Customers" asp-route-search="@Model.Search" asp-route-sortColumn="Name" asp-route-sortOrder="asc" style="text-decoration: none">
                    <i class="fa-solid fa-angle-up"></i>
                </a>
                <a asp-page="/Customers" asp-route-search="@Model.Search" asp-route-sortColumn="Name" asp-route-sortOrder="desc" style="text-decoration: none">
                    <i class="fa-solid fa-angle-down"></i>
                </a>
            </td>
            <td class="col">
                Country
                <a asp-page="/Customers" asp-route-search="@Model.Search" asp-route-sortColumn="Country" asp-route-sortOrder="asc" style="text-decoration: none">
                    <i class="fa-solid fa-angle-up"></i>
                </a>
                <a asp-page="/Customers" asp-route-search="@Model.Search" asp-route-sortColumn="Country" asp-route-sortOrder="desc" style="text-decoration: none">
                    <i class="fa-solid fa-angle-down"></i>
                </a>
            </td>
            <td>
            </td>

            @foreach (var customer in Model.Customers)
            {
                <tr>
                    <td>@customer.Id</td>
                    <td>@customer.Name</td>
                    <td>@customer.Country</td>
                    <td>
                        <a asp-page="/Customers" asp-route-Id="@customer.Id">View</a>
                    </td>
                </tr>
            }
            <tr>
                <td>
                    @if (Model.CurrentPage != 1)
                    {
                        <a class="btn btn-secondary" asp-page="/Customers"
                           asp-route-search="@Model.Search"
                           asp-route-sortOrder="@Model.SortOrder"
                           asp-route-sortColumn="@Model.SortColumn"
                           asp-route-pageNumber="@(Model.CurrentPage-1)">
                            Prev
                        </a>
                    }
                </td>
                <td class="text-center" colspan="2">
                    @for (int i = Math.Max(1, Model.CurrentPage - 3); i <= Math.Min(Model.PageCount, Model.CurrentPage + 3); i++)
                    {
                        <a class="btn @(Model.CurrentPage == i ? "btn-primary" : "btn-secondary")" asp-page="/Customers"
                           asp-route-search="@Model.Search"
                           asp-route-sortOrder="@Model.SortOrder"
                           asp-route-sortColumn="@Model.SortColumn"
                           asp-route-pageNumber="@i">
                            @i
                        </a>
                    }
                </td>

                <td>
                    @if (Model.CurrentPage != Model.PageCount)
                    {
                        <a class="btn btn-secondary" asp-page="/Customers"
                           asp-route-search="@Model.Search"
                           asp-route-sortOrder="@Model.SortOrder"
                           asp-route-sortColumn="@Model.SortColumn"
                           asp-route-pageNumber="@(Model.CurrentPage+1)">
                            Next
                        </a>

                    }
                </td>
            </tr>
        </table>
    </div>
</section>