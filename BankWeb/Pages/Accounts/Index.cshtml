@page
@model IndexModel
@{
}
<div class="container">
    <div class="card">
        <div class="card-header">
            <h1>All Accounts <a href="javascript:history.back()" class="btn btn-primary mt-3 float-right">Back</a></h1>
        </div>
        <div class="card-body">
            <table class="table">
                <tr>
                    <th>Account number</th>
                    <th>Balance</th>
                    <th>Country</th>
                    <th></th>
                    <th></th>
                    <th></th>
                </tr>
                @foreach (var acc in Model.Accounts)
                {
                    <tr>
                        <td>@acc.AccountNumber</td>
                        <td>@acc.Balance</td>
                        <td>@acc.Country</td>
                        <td>
                            <a asp-page="Withdraw" asp-route-accountId="@acc.Id">Withdraw <i class="fa-solid fa-money-bill-transfer"></i></a>
                        </td>
                        <td>
                            <a asp-page="Deposit" asp-route-accountId="@acc.Id">Deposit <i class="fa-solid fa-money-bill-transfer"></i></a>
                        </td>
                        <td>
                            <a asp-page="Manage" asp-route-accountId="@acc.Id">Manage</a>
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
</div>